<!DOCTYPE html>
<html>
<head>
  <title>User Registration</title>
</head>
<body>
  <h2>User Registration Form</h2>
  <form method="POST" action="/submit" enctype="multipart/form-data">
    <div>
      <label>Username:</label>
      <input type="text" name="username" value="<%= old.username || '' %>"/>
      <span style="color:red"><%= errors.username || '' %></span>
    </div>
    <div>
      <label>Password:</label>
      <input type="password" name="password"/>
      <span style="color:red"><%= errors.password || '' %></span>
    </div>
    <div>
      <label>Confirm Password:</label>
      <input type="password" name="confirmPassword"/>
      <span style="color:red"><%= errors.confirmPassword || '' %></span>
    </div>
    <div>
      <label>Email:</label>
      <input type="email" name="email" value="<%= old.email || '' %>"/>
      <span style="color:red"><%= errors.email || '' %></span>
    </div>
    <div>
      <label>Gender:</label>
      <input type="radio" name="gender" value="Male" <%= old.gender === 'Male' ? 'checked' : '' %>/> Male
      <input type="radio" name="gender" value="Female" <%= old.gender === 'Female' ? 'checked' : '' %>/> Female
      <span style="color:red"><%= errors.gender || '' %></span>
    </div>
    <div>
      <label>Hobbies:</label>
      <input type="checkbox" name="hobbies" value="Reading" <%= old.hobbies?.includes('Reading') ? 'checked' : '' %>/> Reading
      <input type="checkbox" name="hobbies" value="Gaming" <%= old.hobbies?.includes('Gaming') ? 'checked' : '' %>/> Gaming
      <input type="checkbox" name="hobbies" value="Traveling" <%= old.hobbies?.includes('Traveling') ? 'checked' : '' %>/> Traveling
      <span style="color:red"><%= errors.hobbies || '' %></span>
    </div>
    <div>
      <label>Upload Profile Picture:</label>
      <input type="file" name="profilePic"/>
      <span style="color:red"><%= errors.profilePic || '' %></span>
    </div>
    <div>
      <label>Upload Other Pictures:</label>
      <input type="file" name="otherPics" multiple/>
    </div>
    <br/>
    <button type="submit">Register</button>
  </form>
</body>
</html>

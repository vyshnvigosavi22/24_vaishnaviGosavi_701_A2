<!DOCTYPE html>
<html>
<head>
  <title>Registration Success</title>
</head>
<body>
  <h2>User Details</h2>
  <table border="1" cellpadding="10">
    <tr><th>Username</th><td><%= data.username %></td></tr>
    <tr><th>Email</th><td><%= data.email %></td></tr>
    <tr><th>Gender</th><td><%= data.gender %></td></tr>
    <tr><th>Hobbies</th><td><%= data.hobbies.join(', ') %></td></tr>
    <tr><th>Profile Picture</th>
      <td><img src="/uploads/<%= images.profilePic.filename %>" width="100"/></td>
    </tr>
    <tr><th>Other Pictures</th>
      <td>
        <% images.otherPics.forEach(pic => { %>
          <img src="/uploads/<%= pic.filename %>" width="100" style="margin:5px"/>
        <% }) %>
      </td>
    </tr>
  </table>
</body>
</html>
